import { graphql, useStaticQuery, Link } from 'gatsby';
import React, { useState } from 'react';

function Footer() {
  return (
    <div>
      footer
      <dir></dir>
    </div>
  );
}

export default Footer;
